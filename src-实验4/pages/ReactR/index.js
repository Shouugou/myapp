import React, { Component } from "react";

export default class ReactR extends Component {
    render() {
        return (
            <div>
                React组件
            </div>
        )
    }
}